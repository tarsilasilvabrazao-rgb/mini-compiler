VAR nota = 15 : INTEIRO.

SE (nota >= 18) {
    EXIBIR("Excelente").
} SENAO SE (nota >= 14) {
    VAR n = 10: INTEIRO.
    EXIBIR(n).
    EXIBIR("Bom").

} SENAO SE (nota >= 10) {
    EXIBIR("Suficiente").
} SENAO {
    EXIBIR("Insuficiente").
}
EXIBIR(n).